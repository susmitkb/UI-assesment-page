import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const maroon = Color(0xFF8B0101); // Total earnings card
  static const maroonDark = Color(0xFF5A0A0D); // kept for future use

  // Surfaces & content
  static const surface = Color(0xFFFFFFFF);
  static const scaffold = Color(0xFFF9FAFB);
  static const onSurface = Color(0xFF1A1A1A);
  static const onSurfaceVariant = Color(0xFF666666);
  static const outline = Color(0xFFEEEEEE);

  // Accents
  static const positive = Color(0xFF34C759);
  static const negative = Color(0xFFFF3B30);
  static const pillBg = Color(0xFF8D1A1E);

  // Misc
  static const iconInactive = Color(0xFF888888);
  static const avatarBg = Color(0xFFF6E9EA);
}
